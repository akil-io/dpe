{
  "apps": [{
    "name": "dpe",
    "exec_mode": "cluster",
    "instances": 1,
    "script": "./bin/dpe-service",
    "env": {
      "DEBUG": "dpe*"
    },
    "args": "--daemon --modules default"
  }]
}